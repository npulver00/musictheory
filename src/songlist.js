// import React, { Component } from "react";

// class songlist extends Component{



//  render() {
//         return (
//           <div>
//               <h1>songlist</h1>
//           </div>
//         )
//       }
//     }

// export default songlist;
// in the app. js//
// handlePost(value){
  //   axios.post('/api/peacefulQuotes', {value}).then(response =>{
  //     console.log(response.data)
  //     this.setState({phrase:response.data})
  //   })

  // }

  // inputPhrase(val){
  //   console.log(val)
  //   this.setState({ type: val }); 
  //   }

  // app.js above render//

    {/* Move below into a separate component */}
                {/* <div className= "boxData">
              <input className ="Choose a word" 
                    placeholder = "words of wisdom"
                    value = {this.state.type} onChange = {(event) => this.inputPhrase(event.target.value)} />
                    <button onClick={()=> this.handlePost(this.state.type)}>Submit New Quote</button>
                    </div> */}

                    //in the render return HTML//

                      {/* <input className ="savedComment" 
                   placeholder = "~~~"
                        value = {this.state.type} onChange = {(event)=> this.addedComment(event.target.value)} />  */}
                         {/* <button onClick={(event) => this.inputComment(this.state.type)} className= "Submit"> Submit </button> */}